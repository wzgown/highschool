server:
  host: 0.0.0.0
  port: 3000
  mode: production

database:
  host: postgres
  port: 5432
  name: highschool
  user: highschool
  password: HS2025!db#SecurePass88
  ssl_mode: disable
  max_conns: 10

redis:
  host: redis
  port: 6379
  password: "HS2025!redis#SecurePass88"
  db: 0

log:
  level: info
  format: json

tracing:
  enabled: true
  service_name: highschool-backend
  # OpenObserve 直连（OTLP/HTTP）；headers 与 compose 中 ZO_ROOT_USER_* 对应
  otlp_endpoint: openobserve:5080
  protocol: http
  url_path: /api/default/v1/traces
  headers: "Authorization=Basic YWRtaW5AaGlnaHNjaG9vbC5sb2NhbDpIaWdoc2Nob29sIzIwMjZvbw=="
  sample_rate: 0.1

tip:
  enabled: true
  qr_url: ""
  review_versions: []

feature:
  agent_enabled: true
  review_versions: []
